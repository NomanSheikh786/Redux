import { combineReducers } from "redux";
import {todoReciving} from "./reducer/TodoReducer"




// const counter = useSelector(state => state.counter)
const rootReducer = combineReducers({
    todoReciving

})


export default rootReducer